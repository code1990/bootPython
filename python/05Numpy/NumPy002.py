# NumPy 安装
# 在 Windows 上，最简单的方法是下载以下的 Python 发行版，它们包含了所有的关键包（包括 NumPy，SciPy，matplotlib，IPython，SymPy 以及 Python 核心自带的其它包）：
#
# Anaconda: 免费 Python 发行版

# python -m pip install --user numpy scipy matplotlib ipython jupyter pandas sympy nose

from numpy import *
# 生成对角矩阵
print(eye(4))
